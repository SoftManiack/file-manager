
import { defineStore } from 'pinia'
import { IDirectory } from '@/model/Directory'
import directoriesApi from "@/api/auth"

export const useDirectoriesStore = defineStore('directories', {
    state: () => {
        return {

        }
    },
    actions: {

    },
    getters: {
        
    }
})